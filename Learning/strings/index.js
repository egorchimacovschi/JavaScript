let userName = "Egor Chimacovschi";

console.log(userName.charAt(0)); //first letter
userName.indexOf("g")
userName.length
userName.lastIndexOf("o")
userName.trim(); // deletes the spaces
userName.toLowerCase();
userName.toUpperCase();
userName.repeat(3);
userName.startsWith("E"); // boolean return

let phoneNumber = "060-711-277"
phoneNumber.replace("-", ""); //eliminates dashes

phoneNumber.padStart(20, "0") // is paddign the string until the string is 20 characters long