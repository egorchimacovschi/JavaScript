// foreach let use use a function for every element of the array

let numbers = [1, 2, 3, 4, 5];

numbers.forEach(display);

function display(element, index, array){
    console.log(element);
    console.log(index);
    console.log(array);
}